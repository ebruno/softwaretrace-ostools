container:
  stage: build_envs
  parallel:
    matrix:
      - RUNNER_TAG: [debian]
  script:
    - |
       env | grep CI | sort;
       pwd;
  allow_failure:
    exit_codes:
      - 1

  tags:
     - $RUNNER_TAG

  rules:
    - changes:
         - .gitlab-ci.yml
         - .gitlab-ci/containers.yml
         - ./system_management/build_containers/**/Containerfile
         - ./system_management/build_containers/**/Doxyfile
         - ./system_management/build_containers/packages/*
      when: on_success
    - when: never

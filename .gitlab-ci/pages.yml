pages:
  stage: publish
  tags:
    - fedora42
  script:
    - |
      mkdir .public;
      cp -r doc/childmgt_c .public
      echo "<html><title>Softwaretrace OS Tools</title><body><p><a href=\"childmgt_c/html/index.html\">Child Managment</a></p></body></html>" > .public/index.html;
      mv .public public
  artifacts:
    paths:
      - public
  rules:
#    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
   - changes:
       - .gitlab-ci.yml
       - .gitlab-ci/pages.yml
